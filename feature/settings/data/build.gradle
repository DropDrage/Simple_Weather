androidLibrary(project)

android {
    namespace 'com.dropdrage.simpleweather.settings.data'
}

dependencies {
    implementation androidLibs.androidx.annotation

    api androidLibs.kotpref
    implementation androidLibs.kotpref.enum
}
