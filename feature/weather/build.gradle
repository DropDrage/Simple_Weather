library_android(
    project,
    bundle_uiScreen,
    component_android_unitTests,
    testOptions_unitTests_returnDefaultValues,
)

apply plugin: 'androidx.navigation.safeargs.kotlin'

android {
    namespace 'com.dropdrage.simpleweather.feature.weather'
}

dependencies {
    implementation androidLibs.androidx.constraintlayout

    implementation androidLibs.collapsingtoolbarlayoutSubtitle

    implementation androidLibs.services.location

    implementation projects.adapters
    implementation projects.common.domain
    implementation projects.common.presentation
    implementation projects.common.util
    implementation projects.core.domain
    implementation projects.core.presentation
    implementation projects.core.style
    implementation projects.core.util
    implementation projects.data.settings
    implementation projects.feature.city.domain
    implementation projects.feature.weather.domain

    testImplementation testLibs.coroutines
    testImplementation testLibs.junit5.params
    testImplementation testLibs.turbine
    testImplementation testLibs.truth

    testImplementation testFixtures(projects.common.test)
}
