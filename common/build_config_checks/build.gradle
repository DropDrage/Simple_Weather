library_android(project)

android {
    namespace 'com.dropdrage.common.build_config_checks'

    testFixtures {
        enable = true
    }
}

dependencies {
    implementation androidLibs.androidx.annotation

    testFixturesImplementation testLibs.mockk.core
    testFixturesImplementation testLibs.mockk.agent
}
