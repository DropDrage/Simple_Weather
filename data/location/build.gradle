library_android(
    project,
    component_android_hilt, component_android_unitTests,
)

android {
    namespace 'com.dropdrage.simpleweather.data.location'
}

dependencies {
    implementation androidLibs.bundles.location

    implementation project(':core:domain')
    implementation project(':common:build_config_checks')
    implementation project(':feature:weather:domain')

    testImplementation testLibs.coroutines

    testImplementation testFixtures(project(':common:test'))
}
